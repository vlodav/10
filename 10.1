#include <iostream>
using namespace std;
int foo(int a)
{
    if (a == 1)
        return 1;
    return 2 * foo(a - 1);
}
int main() {
    setlocale(LC_ALL, "ru");
    cout << "введите номер члена ряда который хотите узнать: ";
    int a;
    cin >> a;
    cout <<"член ряда: "<<a<<" = " << foo(a);
    return 0;
